{
  "name": "fitness-store-frontend",
  "version": "0.0.0",
  "private": true,
  "description": "Front-end test suite for the Mastering Fitness Django app (meso designer + athlete logger).",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.9",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.9"
  }
}
