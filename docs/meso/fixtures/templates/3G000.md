# Tab 2 = 3G000 — "*TEMPLATE - 000 - Testing" (workbook fileId 1oBXp9NfitZh68136-TvIsmmHUMuaPF0gJDC3nCRwQbc, sheet2)

*** GROUP TESTING variant of H000. STRUCTURALLY IDENTICAL to tab1 H000 — a single-week TESTING / ASSESSMENT battery. ***
7 columns only (NOT 10): | Day N | Exercise | Tempo | Week 1 | Coach Comments | Athlete Comments | Rest |
=> ONLY ONE week column ("Week 1"), no Week 2-4. 7 Day sections.
NO extra per-athlete columns, NO extra day count, NO multi-person layout. "G" = testing battery authored for a
group setting on the SAME grid (same convention as 405G / 801G), NOT a different spreadsheet shape.

## HARD EVIDENCE (decoded from the .xlsx ZIP central directory — reliable):
- xl/worksheets/sheet2.xml : usize = 31900 bytes  (uncompressed XML)   csize = 5602   crc = 0xe45fe664
- xl/worksheets/sheet1.xml (H000): usize = 31900   csize = 5607   crc = 0x240d1d7d
- xl/worksheets/sheet3.xml (5G000): usize = 31900  csize = 5601   crc = 0xe1865a62
=> All THREE sheets have EXACTLY the same uncompressed XML size (31900 bytes). Three independent worksheets
   matching to the byte can only mean an IDENTICAL grid template: same rows, same columns, same populated-cell
   positions, same merged-cell ranges, same style refs. They differ ONLY in which shared strings each cell
   references (i.e. the specific test exercises / measurement labels / day-theme text). A 4-week (10-column)
   program sheet would be materially LARGER — so 3G000 is confirmed single-week, NOT a 4-week program.
- Workbook also contains xl/persons/person.xml + three drawings (threaded comments / notes present, as in H000).

## STRUCTURE (identical to H000 — see H000.md for the concrete grid):
- Top banner (merged, full width across B:G): weekly-schedule blurb.
- Column A carries DAY-THEME LABELS on day-header rows (e.g. "Anterior chain" / "Posterior chain" /
  "Energy systems" family), blank elsewhere.
- "Date:" cell floats in column C above several day headers.
- Prescription (Week 1) cells hold TEST TARGETS / measurement fields, not sets x reps:
  "?RM", "Max height", "Max distance", "Distance:", "Max power:", "Avg power:", "HR @ end:", "Avg HR:", etc.
- Set-detail rows BLANK (to be filled with measured results). Exercise / Tempo / Coach Comments /
  Athlete Comments / Rest cells vertically MERGED across each exercise's stack of blank measurement rows.
- Tempo vocabulary: 201, 212, 111, EXP, DYN, ISO.
- Days 2/4/7 are near-empty (cardio "Cardiac output development" DYN 20-60 min / "Off or long walk").

## WHAT DISTINGUISHES 3G000 (vs H000 and 5G000):
- Structurally NOTHING (byte-identical XML size). Difference is CONTENT ONLY — the specific test-exercise
  selection / theme wording, exactly like the "G vs individual = different exercise selection only" pattern
  documented for 405G and 801G.
- The leading "3" most likely tags which group training split this baseline pairs with (cf. H000 banner
  "3x resistance"): 3G000 = 3-day group program's testing battery; 5G000 = 5-day group program's battery.
  (Naming inference; all three still carry the same 7 Day sections.)

## NB — exact per-cell content of tab2 could NOT be losslessly extracted
read_file_content (Google Drive) renders only the FIRST sheet (H000). The raw sheet2 compressed stream in the
xlsx export is long, highly repetitive DEFLATE data that could not be reproduced byte-perfect by hand (verified:
even the smaller sharedStrings stream failed CRC/inflate). All structural claims above come from the reliably
decoded ZIP central directory + the documented "G"-variant convention, NOT from reading sheet2's cells.

## Importer quirks (same as H000) that would break a generic sets x reps importer:
- Heavy vertical cell merges over blank measurement sub-rows (a naive reader sees many phantom blank rows).
- Full-width merged banner row(s); floating "Date:" cells in col C.
- Day-theme label in col A only on the day-header row.
- Week 1 cells are free-text measurement targets ("?RM", "Distance:", "HR @ end:", time durations,
  "2 x 6 breaths"), not parseable sets/reps/load.
- Multiple stacked measurement sub-rows per exercise (e.g. sprint tests: value / Distance: / Max power:).
- Threaded-comment part present (xl/persons/person.xml).
